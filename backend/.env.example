PORT=5000
MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/assessment-platform
JWT_SECRET=your-long-random-secret
JWT_EXPIRE=7d
CLIENT_URL=https://meek-cuchufli-d36247.netlify.app
# Optional if using *.netlify.app — backend also allows Netlify URLs automatically
# Comma-separated for multiple Netlify URLs (e.g. preview deploys)
